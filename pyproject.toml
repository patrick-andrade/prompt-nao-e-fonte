[project]
name = "minicurso-semana-economia-2026"
version = "0.1.0"
description = "Minicurso Prompt não é fonte — Semana da Economia (contrato v1.0)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.2.0",
]

[tool.uv]
package = false
