# Os três HTMLs autossuficientes já estão versionados. O build apenas os confere.
# PPTX, slop e deck de aula não entram no site.

[build]
  publish = "outputs/revealjs-netlify"
  command = "bash scripts/netlify_build.sh"
